# neural-cnc

Projet Tailwind CSS v4 avec PostCSS.

## Setup

```bash
npm install
npm run build:css
```

## Scripts

- `npm run build:css` — compile `src/input.css` vers `dist/output.css`
- `npm run watch:css` — recompile automatiquement à chaque modification